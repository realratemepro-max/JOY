rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow admins to upload images
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
